FROM nginx:stable

RUN rm /etc/nginx/conf.d/nginx.conf
RUN rm /etc/nginx/conf.d/default.conf
COPY nginx.conf /etc/nginx/conf.d

CMD ["/bin/bash", "-c", "envsubst '$${WEB_HOST} $${WEB_DOCKERNAME}' < /etc/nginx/conf.d/nginx.conf > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"]
